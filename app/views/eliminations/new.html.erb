<% provide(:title, @elimination.name) %>

<div class="container-lg my-3">
  <h1 class="mb-5">新しいトーナメント</h1>
</div>


<div class="container-lg bg-light py-3">

  <%= form_with(model: @elimination) do |form| %>
    <%# Error Message %>
    <%= form_error_notification(@elimination) %>

    <%# トーナメント名 %>
    <div class="mb-4">
      <%= form.label(:name, "トーナメント名", class: "form-control-label") %>
      <%= form.text_field(:name, class: "form-control") %>
    </div>

    <%# チーム数 %>
    <div class="mb-4">
      <%= label_tag :num_of_teams, "参加チーム数", class: "form-control-label" %>
      <%= select_tag :num_of_teams, options_for_select(1..MAX_TEAMS, 4), { class: "form-control" } %>
    </div>

    <%# 作成ボタン %>
    <div class="d-flex mt-4 justify-content-end">
      <div class="bg-white">
        <%= form.submit("作成", class: "btn btn-outline-main") %>
      </div>
    </div>

  <% end %>
</div>
